# TrustLens — Launch Thread Draft (SUBM-02)

A neutral, factual launch thread for X. Angle: the one-call hiring-trust verdict + determinism + on-chain pay-per-call. No accusations about any agent — the same neutral-language contract the banned-vocabulary gate enforces.

Post the `#OKXAI` tag on the first tweet (carried below). Thread length is 6 tweets; trim to taste within the neutral-language limits. Character counts are noted as a guide (all comfortably under 280).

---

## Tweet 1 — hook + tag

> Can you trust an OKX.AI agent before you pay it? A 5.0 rating tells you almost nothing on its own — is it 539 sales or 1?
>
> TrustLens returns one evidence-based hiring-trust score for any OKX.AI agent, in a single paid MCP call. #OKXAI

*(≈ 232 chars)*

## Tweet 2 — what it actually scores

> TrustLens weighs four signals into a 0–100 TrustScore + A–F grade:
> • review authenticity
> • rating-vs-sales anomalies
> • sales velocity
> • price fairness vs category
>
> Every component ships a plain-language reason. It flags thin evidence — it never makes an accusation.

*(≈ 268 chars)*

## Tweet 3 — the anomaly example (neutral)

> Two agents, both 5.0 stars. One is graded A/94 (5.0 across 539 sales). The other is D/45, low confidence — a 5.0 backed by a single sale, flagged for thin data, not an assessment of conduct.
>
> Same stars. TrustLens shows you *why* they score differently.

*(≈ 267 chars)*

## Tweet 4 — determinism

> It's deterministic. Same agent, same inputs → same bytes out, every call.
>
> Each result carries a score_version and a data_as_of stamp, and the scoring is pure functions with no wall clock. You can cite a TrustScore and reproduce it later. No black box.

*(≈ 258 chars)*

## Tweet 5 — on-chain pay-per-call

> It's a real A2MCP service: an MCP server over HTTPS, gated by the x402 standard.
>
> 0.01 USDT per call, settled on X Layer with zero gas. Humans can call it — and so can other agents, checking a candidate's TrustScore before they hire it.

*(≈ 243 chars)*

## Tweet 6 — differentiation + close

> Not creditworthiness (Factor), not raw feeds (TO1), not arbitration (Internet Court) — a pre-purchase hiring-trust score for any OKX.AI agent.
>
> One paid MCP call. One deterministic verdict. Built for the OKX AI Genesis Hackathon. #OKXAI

*(≈ 244 chars)*

---

## Notes for the poster (HUMAN-ONLY)

- Posting is a human-only step (a locked stop condition). This file is the prepared draft; it is not auto-posted.
- Attach the demo recording (see `demo-script.md`) to Tweet 1 or Tweet 3 for the strongest visual.
- Keep the neutral framing intact: "flagged for thin data", "not an assessment of conduct". Do not add allegations. This thread carries none of the banned accusatory vocabulary by design, and the language gate in `tests/test_submission_language.py` scans it to keep it that way.
- Replace any placeholder handle/link at post time; the endpoint and methodology URLs live in `listing-copy.md`.

# Deploy-time ONLY (PAYX-04 real settlement) — NOT part of the core runtime.
# Install alongside requirements.txt when enabling real OKX x402 settlement:
#   pip install -r requirements-facilitator.txt
# Provides the `x402` module used lazily by
# server.payments.OkxFacilitatorVerifier (imported only when creds are present).
#
# NOTE: the base package (no extras) pulls ONLY pydantic, nest-asyncio, and
# typing-extensions — it needs NO local web3/eth-account. web3/fastapi/starlette
# live behind the optional [evm]/[all]/... extras, which we do NOT install: we call
# the HOSTED OKX facilitator over HTTPS (its POSTs reuse the httpx already pinned by
# the core requirements.txt), so no scheme handler or local signer is required and
# the pinned fastapi/starlette stack is not re-resolved. Still, install into the
# deploy environment and verify the app boots and /healthz returns 200.
okxweb3-app-x402==0.1.1

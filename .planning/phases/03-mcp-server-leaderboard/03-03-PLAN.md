---
phase: 03-mcp-server-leaderboard
plan: 03
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - indexer/refresh.py
  - pyproject.toml
  - .gitignore
  - tests/test_refresh_scores.py
  - tests/test_refresh_web.py
  - tests/test_refresh.py
autonomous: true
requirements: [WEB-03]

must_haves:
  truths:
    - "python -m indexer.refresh (CLI, default args) regenerates web/dist/index.html from SQLite in the same run that indexes and scores"
    - "Rerunning the CLI produces byte-identical HTML (determinism preserved end-to-end)"
    - "The refresh exit-code taxonomy is unchanged: 0 success, 1 csv problem, 2 environment problem — a failed page build exits 2"
    - "Library calls refresh(csv, db, ts) without web_out write NO page file; the two exit-0 CLI tests in tests/test_refresh.py pass explicit --web-out into tmp_path — after the suite runs, nothing has written into the repo tree"
    - "RefreshSummary fields are unchanged (frozen contract: agents, snapshots_appended, field_warnings, source)"
  artifacts:
    - path: "indexer/refresh.py"
      provides: "web_out parameter threading + --web-out CLI flag + web.build invocation after commit"
      contains: "DEFAULT_WEB_OUT"
    - path: "pyproject.toml"
      provides: "packages list extended to indexer, scoring, server, web"
      contains: "\"server\", \"web\""
    - path: ".gitignore"
      provides: "web/dist/ ignored"
      contains: "web/dist/"
    - path: "tests/test_refresh_web.py"
      provides: "CLI regeneration + HTML byte-determinism + exit-2 + no-write-without-web_out tests"
  key_links:
    - from: "indexer/refresh.py"
      to: "web/build.py"
      via: "module import called in _persist_records AFTER the transaction commits and conn.close() runs"
      pattern: "from web.build import build"
    - from: "main() --web-out default"
      to: "web/dist/index.html"
      via: "DEFAULT_WEB_OUT constant"
      pattern: "web/dist"
---

<objective>
Wire the leaderboard builder into the refresh pipeline so the page auto-regenerates from SQLite on every `python -m indexer.refresh` (WEB-03), while preserving the exit-code contract, the byte-determinism guarantees, the frozen RefreshSummary, and test isolation (research Pitfall 7). Also extend pyproject packaging to `server` + `web` (required before the Docker image build in plan 03-05) and gitignore the generated artifact.

Purpose: WEB-03 — "Page content auto-regenerates from SQLite on indexer refresh."
Output: modified refresh pipeline + regression tests. No server code.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-mcp-server-leaderboard/03-CONTEXT.md
@.planning/phases/03-mcp-server-leaderboard/03-RESEARCH.md
@indexer/refresh.py
@tests/test_refresh_scores.py
@pyproject.toml

<interfaces>
<!-- Current signatures being extended (from indexer/refresh.py as of Phase 2): -->
```python
DEFAULT_CSV = Path("data/okx-marketplace-census-2026-07-10.csv")
DEFAULT_DB = Path("data/trustlens.db")

def persist(conn, records, captured_at, source="census") -> None            # UNTOUCHED
def _persist_records(db_path, records, field_warnings, captured_at,
                     source="census", generated_at=None) -> RefreshSummary  # gains web_out
def refresh(csv_path, db_path, captured_at, source="census",
            generated_at=None) -> RefreshSummary                            # gains web_out
def main(argv=None) -> int                                                  # gains --web-out
# main() error mapping (KEEP): load_census errors -> 1; (OSError, sqlite3.Error)
# around _persist_records -> 2
```

From plan 03-01 (wave 1, complete):
```python
from web.build import build  # build(db_path, out_path, base_url="http://localhost:8000") -> int (bytes)
```

Existing call sites that must keep working UNCHANGED:
- tests/test_refresh.py:46,75,76 — `refresh(CSV_PATH, db, TS)` library calls (no web_out -> must write NO page)
- tests/test_refresh_scores.py:29 — `main(["--csv", ..., "--db", ...])` via the `_run` helper (THIS helper gets the --web-out addition)

Locked planner decision (orchestrator resolved research Open Question 1): `web_out: Path | None = None` and **None means SKIP the page build** for library calls; `main()` defaults `--web-out` to `DEFAULT_WEB_OUT = Path("web/dist/index.html")` and always passes it through — so the CLI (the WEB-03 surface) always regenerates. Library-call tests stay untouched, but tests/test_refresh.py's CLI section has TWO exit-0 `main()` invocations that would hit the always-build default (`test_cli_main` at ~line 278 and the second, exit-0 invocation inside `test_cli_dateless_filename_returns_2` at ~lines 306-308) — Task 2 edits exactly those two call sites to pass `--web-out` into tmp_path (the same treatment as test_refresh_scores.py's `_run` helper). The exit-1/exit-2 invocations never reach the build and need no edit.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Thread web_out through refresh + extend packaging and gitignore</name>
  <files>indexer/refresh.py, pyproject.toml, .gitignore</files>
  <read_first>
    - indexer/refresh.py (ENTIRE file — the exit-code taxonomy in the module docstring and main() is a locked contract)
    - .planning/phases/03-mcp-server-leaderboard/03-RESEARCH.md — section "Leaderboard → refresh wiring (WEB-03) — preserve the exit-code contract" (wiring point, log line, failure taxonomy — transcribe the log call VERBATIM) and "Common Pitfalls §7" (tests writing into the repo)
    - .planning/phases/03-mcp-server-leaderboard/03-CONTEXT.md — "Claude's Discretion" bullet (import preferred over shelling out; keep exit-code contract) and "Leaderboard (locked)" (builder invoked at the end of indexer.refresh writing web/dist/index.html, gitignored)
  </read_first>
  <action>
**indexer/refresh.py** — minimal, surgical edits:

1. Top-of-file: add `import os` and `from web.build import build as web_build` (import preferred — locked discretion note). Add constant `DEFAULT_WEB_OUT = Path("web/dist/index.html")` next to DEFAULT_CSV/DEFAULT_DB and add `"DEFAULT_WEB_OUT"` to `__all__`.
2. `_persist_records(...)`: add keyword param `web_out: Path | None = None`. AFTER the existing `finally: conn.close()` block (build reads committed rows via its own connection — research wiring point) and BEFORE constructing RefreshSummary, insert:

```python
if web_out is not None:
    size = web_build(
        db_path, web_out,
        base_url=os.environ.get("TRUSTLENS_BASE_URL", "http://localhost:8000"),
    )
    log.info(
        "leaderboard built: %s (%d agents, %d bytes)", web_out, len(records), size
    )
```

3. `refresh(...)`: add `web_out: Path | None = None` and pass it through to `_persist_records`.
4. `main()`: add argparse arg:

```python
parser.add_argument(
    "--web-out", type=Path, default=DEFAULT_WEB_OUT,
    help=f"leaderboard html output path (default: {DEFAULT_WEB_OUT})",
)
```

   and pass `web_out=args.web_out` in the existing `_persist_records(...)` call — INSIDE the existing `try/except (OSError, sqlite3.Error) -> return 2` block. A page-build failure is an environment problem, same class as a DB failure (research failure taxonomy: build raises OSError -> caught -> exit 2). Do NOT add new except clauses.
5. Module docstring: extend the exit-codes line to mention the page build: "2 captured-at underivable, database failure, or leaderboard build failure (environment problem)". Do NOT touch RefreshSummary (frozen — tests/test_refresh_scores.py pins its exact fields), do NOT touch `persist()`, do NOT reorder existing logic.

**pyproject.toml** — `[tool.setuptools]` packages becomes:

```toml
packages = ["indexer", "scoring", "server", "web"]
```

(The comment line above it — "Phase 3 adds server to this list" — can be removed or updated.) Then run `pip install -e .` so the editable install picks up the new packages (required for `uvicorn server.main:app` and the Docker `pip install .` in later plans).

**.gitignore** — under the "Runtime artifacts (regenerated by indexer)" section, add the line `web/dist/`.
  </action>
  <verify>
    <automated>python -c "import tempfile, pathlib; from indexer.refresh import main, DEFAULT_WEB_OUT; d = pathlib.Path(tempfile.mkdtemp()); rc = main(['--db', str(d/'t.db'), '--web-out', str(d/'index.html')]); assert rc == 0 and (d/'index.html').stat().st_size > 51200; print('OK cli builds page, rc=0')"</automated>
  </verify>
  <acceptance_criteria>
    - The automated verify prints `OK cli builds page, rc=0`
    - `grep -c 'DEFAULT_WEB_OUT' indexer/refresh.py` returns >= 3 (constant + __all__ + argparse default)
    - `grep -c 'from web.build import build' indexer/refresh.py` returns 1
    - `grep -c '"server", "web"' pyproject.toml` returns 1
    - `grep -c 'web/dist/' .gitignore` returns 1
    - `pip install -e . 2>&1 | tail -1` reports a successful install
    - `python -c "from indexer.refresh import refresh; import inspect; sig = inspect.signature(refresh); assert sig.parameters['web_out'].default is None; print('OK default None')"` prints `OK default None`
  </acceptance_criteria>
  <done>CLI refresh always regenerates the page (default web/dist/index.html); library calls without web_out build nothing; exit codes 0/1/2 taxonomy intact; packaging covers all 4 packages.</done>
</task>

<task type="auto">
  <name>Task 2: Regression tests — regeneration, HTML byte-determinism, exit 2, isolation</name>
  <files>tests/test_refresh_web.py, tests/test_refresh_scores.py, tests/test_refresh.py</files>
  <read_first>
    - tests/test_refresh_scores.py (the `_run` helper at line 27–29 and the 6 tests using it — the ONLY edit here is the helper; every existing assertion must keep passing)
    - tests/test_refresh.py (its refresh() library calls remain page-free with the None default and MUST NOT be edited; its CLI section needs exactly TWO edits — the exit-0 `main()` call sites in `test_cli_main` (~line 278) and the second invocation in `test_cli_dateless_filename_returns_2` (~lines 306-308) each gain `"--web-out", str(tmp_path / "index.html")` arguments; all assertions stay unmodified)
    - .planning/phases/03-mcp-server-leaderboard/03-RESEARCH.md — "Leaderboard → refresh wiring" (byte-determinism: "rerunning refresh must produce byte-identical HTML — extend the existing rerun-hash test to cover the HTML file")
    - indexer/refresh.py (as modified in Task 1)
  </read_first>
  <action>
**tests/test_refresh_scores.py** — ONE edit, the `_run` helper only:

```python
def _run(db_path, *extra):
    """In-process CLI call over the real census (mirrors test_refresh.py)."""
    return main(["--csv", str(CSV_PATH), "--db", str(db_path),
                 "--web-out", str(Path(db_path).parent / "index.html"), *extra])
```

(add `from pathlib import Path` if not already imported — it is, line 12). No other change in this file; all 6 existing tests must pass unmodified.

**tests/test_refresh.py** — exactly TWO call-site edits, nothing else: in `test_cli_main` (~line 278) change `main(["--db", str(db)])` to also pass `"--web-out", str(tmp_path / "index.html")`; in `test_cli_dateless_filename_returns_2` (~lines 306-308) add the same `--web-out` pair to the SECOND (exit-0) `main()` invocation only. Do not touch the exit-1/exit-2 invocations, the library-call tests, or any assertion.

**tests/test_refresh_web.py** — new file, CLI-level tests (mirror the `_run` pattern):

1. `test_cli_regenerates_page(tmp_path)` — run CLI with `--web-out tmp_path/"index.html"` -> exit 0; file exists; size > 50 KiB; content contains `<tr id="agent-` exactly 272 times, `id="methodology"`, and `id="badge"` (WEB-03: the same single command indexes, scores, AND regenerates)
2. `test_cli_rerun_html_byte_identical(tmp_path)` — run the CLI twice against the SAME db and web_out; capture `hashlib.sha256(html_bytes)` after each run; hashes equal (extends the Phase 2 rerun-determinism contract to the HTML artifact)
3. `test_page_build_failure_exits_2(tmp_path)` — create a FILE at `tmp_path/"blocker"`, then run CLI with `--web-out tmp_path/"blocker"/"index.html"` (parent is a file -> mkdir/open raises OSError on all platforms) -> exit code 2, and the db was still written (environment-problem class, db-side taxonomy preserved)
4. `test_library_refresh_writes_no_page(tmp_path)` — `refresh(CSV_PATH, tmp_path/"t.db", SEED_TS)` (no web_out) -> `list(tmp_path.glob("**/*.html")) == []` (Pitfall 7 isolation contract pinned)
5. `test_refresh_summary_unchanged_by_wiring(tmp_path)` — `_persist`-path sanity: CLI run returns 0 and the log line `leaderboard built:` appears via caplog at INFO for logger `indexer.refresh` (mirrors test_cli_logs_scores_summary_line style)

Use the same constants as the sibling file: `CSV_PATH = REPO_ROOT / "data" / "okx-marketplace-census-2026-07-10.csv"`, `SEED_TS = "2026-07-10T00:00:00Z"`.
  </action>
  <verify>
    <automated>python -m pytest tests/test_refresh_web.py tests/test_refresh_scores.py tests/test_refresh.py --no-cov -q</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/test_refresh_web.py --no-cov -q` exits 0 with >= 5 tests collected
    - `python -m pytest tests/test_refresh_scores.py tests/test_refresh.py --no-cov -q` exits 0 (existing refresh suites green with the helper edit and the two --web-out call-site edits; no assertion in either file modified)
    - `python -m pytest tests/test_refresh_web.py::test_cli_rerun_html_byte_identical --no-cov -q` exits 0 (determinism pinned)
    - After the full run: `ls web/dist/index.html` fails (tests wrote nothing into the repo tree)
  </acceptance_criteria>
  <done>WEB-03 proven at the CLI level with byte-determinism; exit-code taxonomy pinned for build failures; repo-write isolation pinned; zero regressions in the existing refresh suites.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Census CSV -> generated page | Ingest boundary is Phase 1's (unchanged here); page escaping is plan 03-01's contract — this plan only threads paths |
| CLI args -> filesystem writes | --web-out is operator-controlled (local CLI), not network input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-10 | Tampering (integrity) | exit-code contract | mitigate | Build failures map into the existing (OSError, sqlite3.Error) -> 2 path; test pins exit 2 and taxonomy docstring updated (Task 2 #3) |
| T-03-11 | Elevation/DoS | test suite writing into working tree | mitigate | web_out=None default for library calls + tmp-path CLI tests; explicit no-page-written test (Task 2 #4) and post-run `ls web/dist` check |
| T-03-12 | Information Disclosure | TRUSTLENS_BASE_URL env | accept | Non-secret config with safe default; documented in Phase 4's .env.example (deferred by CONTEXT) |
</threat_model>

<verification>
- `python -m pytest tests/test_refresh_web.py tests/test_refresh_scores.py tests/test_refresh.py --no-cov -q` exits 0
- `python -m indexer.refresh` (real defaults) exits 0 and writes both `data/trustlens.db` and `web/dist/index.html` (this ALSO stages the artifacts plan 03-04's manual smoke and plan 03-05's local Inspector run rely on)
- `git status --porcelain` shows no web/dist/ entries (gitignored)
</verification>

<success_criteria>
- WEB-03: one command (`python -m indexer.refresh`) indexes, scores, and regenerates the page — byte-identical on rerun
- Exit-code contract 0/1/2 preserved and extended-in-meaning only (build failure = 2)
- RefreshSummary frozen; tests/test_refresh.py untouched; no repo writes from tests
- pyproject packages = indexer, scoring, server, web; web/dist/ gitignored
</success_criteria>

<output>
After completion, create `.planning/phases/03-mcp-server-leaderboard/03-03-SUMMARY.md`
</output>

---
phase: 05-scraper-hardening-submission-kit
plan: 03
type: execute
wave: 2
depends_on: [02]
files_modified:
  - submission/demo-script.md
  - submission/x-post-draft.md
  - submission/listing-copy.md
  - tests/test_submission_language.py
autonomous: true
requirements: [SUBM-01, SUBM-02, SUBM-03, OPS-03]
user_setup: []

must_haves:
  truths:
    - "submission/demo-script.md is a 90-second storyboard: problem to live MCP call to a score card with an anomaly flag (GlassDesk 3465 D/45/low, 5.0 on 1 sale vs 3345 A/94/high) to agent-calling-agent flow to leaderboard + on-chain 0.01 USDT/call revenue, executable against a clean-clone docker compose up (Docker-engine start marked HUMAN-ONLY)"
    - "submission/x-post-draft.md is a launch thread tagged #OKXAI: neutral, factual, no accusations, highlighting the one-call hiring-trust verdict + determinism + on-chain pay-per-call"
    - "submission/listing-copy.md has name (TrustLens), a tagline <=80 characters, description, category 'Software Services', price 0.01 USDT, plus endpoint + methodology URL placeholders"
    - "a language-gate test asserts the banned-vocabulary regex (fraud|scam|fake|manipulat) matches nowhere in submission/*.md OR README.md, and asserts the listing tagline is <=80 chars"
    - "the full pytest suite stays green with the new language-gate test added (no coverage-gate change)"
  artifacts:
    - path: "submission/demo-script.md"
      provides: "SUBM-01 90-second demo storyboard with verified real agent data and the flagged-not-accused anomaly beat"
      min_lines: 40
      contains: "GlassDesk"
    - path: "submission/x-post-draft.md"
      provides: "SUBM-02 launch thread with #OKXAI, neutral/factual"
      contains: "#OKXAI"
    - path: "submission/listing-copy.md"
      provides: "SUBM-03 ASP listing fields incl. a <=80-char tagline and category Software Services at 0.01 USDT"
      contains: "Software Services"
    - path: "tests/test_submission_language.py"
      provides: "Banned-vocab scan over submission/*.md + README.md and the <=80-char tagline assertion"
      min_lines: 30
  key_links:
    - from: "tests/test_submission_language.py"
      to: "submission/*.md and README.md"
      via: "reads each file and asserts the banned-vocab regex does not match"
      pattern: "fraud.scam.fake.manipulat"
    - from: "submission/listing-copy.md tagline"
      to: "the <=80-char limit"
      via: "test asserts len(tagline) <= 80"
      pattern: "80"
---

<objective>
Produce the submission kit — `submission/demo-script.md` (SUBM-01), `submission/x-post-draft.md` (SUBM-02), `submission/listing-copy.md` (SUBM-03) — using verified real agent data, and add a language-gate test (`tests/test_submission_language.py`) that scans all outward submission text plus README.md for banned vocabulary and enforces the <=80-char tagline limit (OPS-03 hardening). All copy uses neutral analytics language.

Purpose: These are the artifacts a human copies into the OKX listing, the demo recording, and the launch post. The anomaly beat must read as "flagged for thin data, not an assessment of conduct" using a real reason string; the tagline must fit okx.ai's 80-char limit; and the language gate makes the neutral-wording contract mechanically enforced over the outward surface.
Output: three `submission/*.md` files and one test file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-scraper-hardening-submission-kit/05-CONTEXT.md
@.planning/phases/05-scraper-hardening-submission-kit/05-RESEARCH.md

<verified_submission_facts>
<!-- All VERIFIED against the built DB / score_agent in 05-RESEARCH.md, "Submission Kit facts". -->
DEMO HERO (anomaly beat — the money shot):
  - GlassDesk, id 3465 -> D / 45 / low confidence; rating 5.0 on ONLY 1 sale.
  - Contrast: 这个能吃吗？ (id 3345) -> A / 94 / high; rating 5.0 on 539 sales.
  - Both are 5.0 stars — TrustLens shows WHY one is A and one is D.
  - The reason string to READ ALOUD (verified verbatim from the built DB, rating_credibility.reason for 3465):
      "perfect 5.0 rating backed by only 1 sale(s) — pattern consistent with limited
       review history; low confidence, flagged for thin data (not an assessment of conduct)"
  - AVOID Factor Credit Desk (4502) as a subject — it is a named competitor. Backup heroes if needed:
    Token Radar (2991, D/52/low, 5.0 on 3 sales) or Thumbnail Maker (4511, D/45/low, 5.0 on 1 sale).

90s STORYBOARD BEATS:
  1. Problem: "Can you trust a marketplace agent before you pay it?"
  2. Live MCP call: Claude calls score_agent("这个能吃吗？") -> A / 94 / high, 5.0 on 539 sales.
  3. Anomaly flag (money beat): score_agent("GlassDesk") / id 3465 -> D / 45 / low; same 5.0 stars but on 1 sale; read the reason. Two agents, both 5.0 — TrustLens shows why one is A and one is D.
  4. Agent-calling-agent: one agent calls score_agent on another before hiring (the A2MCP flow; same paid call).
  5. Leaderboard + on-chain: show "/" (272 agents, sortable, grades) + the 0.01 USDT/call settling on X Layer (the 402->paid flow from Phase 4).
  - Executable against a clean-clone `docker compose up`; mark the Docker-engine start HUMAN-ONLY.

LISTING (SUBM-03):
  - Name: TrustLens
  - Category: "Software Services" (brief) — okx.ai's real filter code is SOFTWARE_SERVICES, display "Software services".
  - Price: 0.01 USDT/call. Include an endpoint URL placeholder (https://<host>/mcp) + methodology URL (<base>/#methodology).
  - Taglines (<=80 chars — VERIFIED counts, all banned-word-clean; pick ONE primary, list the rest as alternates):
      | Tagline | Chars |
      | Deterministic trust scores for OKX.AI agents, in one paid MCP call | 66 |
      | Evidence-based hiring-trust scores for OKX.AI agents via one paid MCP call | 74 |
      | Hiring-trust scores for OKX.AI agents — one paid MCP call, deterministic JSON | 77 |
      | TrustScore for any OKX.AI agent — evidence-based, deterministic, one paid call | 78 |
      | Should you hire this OKX.AI agent? One paid MCP call, one deterministic verdict | 79 |
      (REJECTED, over-limit: "One paid MCP call returns a deterministic hiring-trust score for any OKX.AI agent" = 81.)
  - Description differentiation line (bake in): a marketplace hiring-trust score (review authenticity,
    rating-vs-sales anomalies, sales velocity, price fairness) — distinct from creditworthiness (Factor),
    raw data feeds (TO1), and arbitration (Internet Court).

X-POST (SUBM-02):
  - Thread with #OKXAI. Neutral, factual, NO accusations (banned-word test spirit applies).
  - Angle: one-call hiring-trust verdict + determinism (score_version + data_as_of + pure functions = same call, same bytes) + on-chain pay-per-call (0.01 USDT on X Layer, zero gas).
  - Differentiation one-liner: "Not creditworthiness (Factor), not raw feeds (TO1), not arbitration (Internet Court) — a pre-purchase hiring-trust score for any OKX.AI agent."
</verified_submission_facts>

<banned_vocab_pattern>
<!-- The exact regex used across the existing suite (test_scoring_golden.py, test_web_build.py, etc.). Reuse it VERBATIM. -->
BANNED = re.compile(r"(?i)(fraud|scam|fake|manipulat)")
The canonical directory-scan pattern to mirror: tests/test_scoring_golden.py::test_banned_vocabulary_source_layer
(the regex literal must live in tests/, outside the scanned tree — scan submission/ + README.md, NOT source code,
so the scan never trips on indexer/category.py's "scam"/"rug" keyword table).
</banned_vocab_pattern>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the three submission/*.md files with verified data + neutral language</name>
  <files>submission/demo-script.md, submission/x-post-draft.md, submission/listing-copy.md</files>
  <read_first>
    - .planning/phases/05-scraper-hardening-submission-kit/05-RESEARCH.md — "Submission Kit facts" (demo beats table with verified data, the 5 char-counted taglines, the x-post angle, the differentiation lines)
    - .planning/PROJECT.md — Core Value + the Factor/TO1/Internet Court differentiation context (for accurate, neutral framing)
    - .planning/phases/05-scraper-hardening-submission-kit/05-CONTEXT.md — the locked SUBM-01..03 decisions (each file's required fields) and the neutral-language + Docker-human-step constraints
  </read_first>
  <action>
Create the `submission/` directory and three files. Use the `<verified_submission_facts>` block as the source of truth — the agent ids, grades, and the reason string are verified against the built DB; do not invent numbers.

**submission/demo-script.md (SUBM-01)** — a 90-second storyboard. Structure as an ordered beat list (beat, ~seconds, on-screen action, narration). Use the 5 beats from `<verified_submission_facts>`:
  - Beat 2 uses score_agent("这个能吃吗？") -> A/94/high on 539 sales.
  - Beat 3 (the money beat) uses score_agent("GlassDesk") / id 3465 -> D/45/low on 1 sale; quote the verified reason string verbatim and have the narrator say the two-agents-both-5.0 line. Frame it as "flagged for thin data, not an assessment of conduct" — neutral, never an allegation.
  - Beat 4 narrates the agent-calling-agent A2MCP flow; Beat 5 shows the leaderboard (272 agents) + the 0.01 USDT/call on-chain settlement (the Phase 4 402->paid flow).
  Add a short "## Setup (run before recording)" note: the script is executable against a clean-clone `docker compose up`; mark **HUMAN-ONLY: start the Docker Desktop engine** (carried Phase-3/4 blocker). Optionally note the backup hero agents. Neutral wording only.

**submission/x-post-draft.md (SUBM-02)** — a launch thread. 3-6 short tweets, the first or last carrying `#OKXAI`. Cover: the one-call hiring-trust verdict, determinism (score_version + data_as_of + pure functions = same call, same bytes), on-chain pay-per-call (0.01 USDT on X Layer, zero gas), and the differentiation one-liner from `<verified_submission_facts>`. Neutral, factual, NO accusations. (Thread length is Claude's discretion within neutral-language limits.)

**submission/listing-copy.md (SUBM-03)** — the ASP listing fields as a labeled list:
  - **Name:** TrustLens
  - **Tagline:** pick ONE primary from the verified <=80-char list (recommend "Deterministic trust scores for OKX.AI agents, in one paid MCP call" — 66 chars) and list the other four as alternates, each with its char count noted. Every tagline used MUST be <=80 chars (Task 2's test enforces the primary).
  - **Description:** a neutral paragraph baking in the differentiation line (hiring-trust vs Factor/TO1/Internet Court) and the 4 signals; link the methodology URL (`<base>/#methodology`).
  - **Category:** Software Services (note okx.ai's display value "Software services").
  - **Price:** 0.01 USDT per call.
  - **Endpoint URL:** placeholder `https://<host>/mcp`.
  Neutral wording only; placeholders for host/base — no real domain.

Across all three: never "fraud/scam/fake/manipulat"; no allegation against any named agent; competitor names appear only in the neutral differentiation contrast, never as a demo subject.
  </action>
  <verify>
    <automated>python -c "import re,pathlib; d=pathlib.Path('submission'); demo=(d/'demo-script.md').read_text(encoding='utf-8'); xp=(d/'x-post-draft.md').read_text(encoding='utf-8'); lc=(d/'listing-copy.md').read_text(encoding='utf-8'); assert 'GlassDesk' in demo and 'not an assessment of conduct' in demo and 'docker compose up' in demo and 'HUMAN-ONLY' in demo; assert '#OKXAI' in xp; assert 'Software Services' in lc and '0.01 USDT' in lc; b=re.compile(r'(?i)(fraud|scam|fake|manipulat)'); assert not (b.search(demo) or b.search(xp) or b.search(lc)); print('submission kit OK')"</automated>
  </verify>
  <acceptance_criteria>
    - All three files exist under `submission/`.
    - demo-script.md contains "GlassDesk", the verbatim reason string fragment "not an assessment of conduct", "docker compose up", and a "HUMAN-ONLY" Docker-engine marker.
    - x-post-draft.md contains "#OKXAI".
    - listing-copy.md contains "Software Services" and "0.01 USDT" and a tagline line.
    - The banned-vocab regex matches in none of the three (asserted here AND by Task 2's gate over the whole set).
    - No named competitor (Factor/TO1/Internet Court) appears as a demo SUBJECT — only in the neutral differentiation contrast.
  </acceptance_criteria>
  <done>submission/ holds the three kit files with verified real data, the flagged-not-accused anomaly beat, the #OKXAI thread, and the Software-Services/0.01-USDT listing with a <=80-char tagline — all neutral, no banned vocabulary.</done>
</task>

<task type="auto">
  <name>Task 2: Add tests/test_submission_language.py — banned-vocab gate + tagline limit</name>
  <files>tests/test_submission_language.py</files>
  <read_first>
    - tests/test_scoring_golden.py (the canonical banned-vocab directory-scan: test_banned_vocabulary_source_layer — mirror its structure; the regex literal lives in tests/, outside the scanned tree)
    - .planning/phases/05-scraper-hardening-submission-kit/05-RESEARCH.md — "Banned-word test extension (recommended)" (scan submission/*.md for fraud|scam|fake|manipulat) and the tagline <=80-char requirement
    - README.md (produced by Plan 02, wave 1 — it exists by the time this wave-2 plan runs; the gate scans it too)
    - the three submission/*.md files just written in Task 1
  </read_first>
  <action>
Create `tests/test_submission_language.py`. Mirror the `test_banned_vocabulary_source_layer` pattern (regex literal in the test file, scan target files by path via `Path(__file__).resolve().parents[1]`).

```python
import re
from pathlib import Path
import pytest

REPO = Path(__file__).resolve().parents[1]
BANNED = re.compile(r"(?i)(fraud|scam|fake|manipulat)")
SUBMISSION = REPO / "submission"
README = REPO / "README.md"
```

Tests:
1. `test_submission_dir_has_the_three_kit_files`: assert `(SUBMISSION / n).is_file()` for `demo-script.md`, `x-post-draft.md`, `listing-copy.md`.
2. `test_banned_vocabulary_absent_from_outward_text`: build the file list = `sorted(SUBMISSION.glob("*.md"))` PLUS `README` if it exists (`if README.is_file(): files.append(README)`). Assert the list is non-empty, then for each file assert `not BANNED.search(path.read_text(encoding="utf-8")), path.name`. (Reading UTF-8 keeps CJK in demo-script.md safe.)
3. `test_listing_tagline_within_80_chars`: read `listing-copy.md`; locate the PRIMARY tagline. Make this robust and self-contained: mark the primary tagline in listing-copy.md on its own line using an unambiguous, greppable marker the test keys on — REQUIRE Task 1 to write the primary tagline as a line of the exact form `**Tagline:** <text>` (single line). In the test: find the line starting with `**Tagline:**`, strip that prefix, strip surrounding whitespace/backticks, and assert `len(tagline) <= 80` and `tagline` (non-empty) and `not BANNED.search(tagline)`. If the marker convention risks ambiguity, instead assert over EVERY tagline row in the verified table by extracting lines between the tagline section markers — but the single `**Tagline:**` primary line is the required, tested contract.

Note in a comment that this test runs inside the normal suite; it is NOT under `--cov=scoring`, so it neither dilutes nor inflates the coverage gate (no `--no-cov` needed for the full run). Keep the regex literal ONLY in this test file so it never scans itself into a false positive is not a concern (the file is under tests/, outside submission/ + README scope).
  </action>
  <verify>
    <automated>python -m pytest tests/test_submission_language.py -q --no-cov</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/test_submission_language.py -q --no-cov` passes: the three files exist, no banned vocab in submission/*.md OR README.md, and the primary tagline is <=80 chars and banned-word-clean.
    - The scan file list includes README.md (proven by the test importing/joining it) — the gate covers the whole outward-text surface, not just submission/.
    - The test does NOT scan any source directory (indexer/scoring/server/web), so it cannot trip on the `indexer/category.py` "scam"/"rug" keyword table.
    - `python -m pytest -q` (full suite) stays green with this test added and the coverage gate unchanged (still >=90% on scoring/).
  </acceptance_criteria>
  <done>tests/test_submission_language.py enforces neutral outward language across submission/*.md + README.md and the <=80-char tagline limit; it runs inside the full suite without touching the coverage gate.</done>
</task>

</tasks>

<verification>
- `python -m pytest tests/test_submission_language.py -q --no-cov` → passes (files present, no banned vocab across submission/ + README, tagline <=80).
- `python -m pytest -q` → full suite green (296 + scraper tests from Plan 01 + this language gate), scoring coverage still >=90%.
- Manual: demo-script beats match the verified data; x-post carries #OKXAI; listing shows Software Services + 0.01 USDT; every human step (Docker engine) marked.
</verification>

<success_criteria>
- `submission/` contains demo-script.md (90-second storyboard: problem → live MCP call → score card with the flagged-not-accused anomaly → agent-calling-agent flow → leaderboard + on-chain revenue), x-post-draft.md (#OKXAI), and listing-copy.md (name, tagline <=80 chars, description, category Software Services, 0.01 USDT) (SUBM-01..03).
- The demo script is executable against a clean-clone `docker compose up`, with the Docker-engine start marked HUMAN-ONLY (the actual rehearsal folds into the final human checklist — the plan produces a script that IS executable, it does not fake an execution).
- Outward language is neutral and mechanically enforced (banned-vocab gate over submission/*.md + README.md; tagline <=80 chars) with no coverage-gate change (OPS-03 hardening).
</success_criteria>

<output>
After completion, create `.planning/phases/05-scraper-hardening-submission-kit/05-03-SUMMARY.md`
</output>
